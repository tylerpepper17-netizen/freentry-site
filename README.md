# freentry-site

Published output for **[freentry.net](https://freentry.net)**.

This repository is **generated**. Every file here is built from the
private `freentry` repository by its `publish` workflow, so any hand
edit made here is overwritten on the next build. Change the source.
